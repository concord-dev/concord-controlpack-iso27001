# Every active identity-provider user is enrolled in a phishing-resistant MFA factor

`ISO27001-A.8.5-secure-authentication` · framework **iso27001** · severity **critical** · Technological Controls

## What this control checks

ISO/IEC 27001:2022 A.8.5 requires secure authentication technologies
appropriate for the risk. Concord queries the IdP and verifies every
ACTIVE user has at least one strong MFA factor (TOTP, push, WebAuthn,
or hardware token).

## Why it matters

SMS and voice MFA are well-documented as bypassable. ISO 27001 audits
increasingly expect phishing-resistant factors, especially after
SIM-swap-driven breaches in 2023-2025.

## Evidence

Collected from the `okta` source (`users_mfa` evidence type).

## What a failure looks like

This control reports a finding when any of the following hold:

- no Okta evidence collected
- active user <value> has no strong MFA factor enrolled
- user <value> still has weak factors (SMS/call/email) enrolled — remove to prevent phishing-fallback

## Remediation

Bring each affected resource or attestation listed under *What a failure looks like* into compliance, then re-collect evidence. Estimated effort: **1d**. Automated fix available: **false**.

## How to re-verify

```
concord check --controls <pack>/controls --framework iso27001 --control-id ISO27001-A.8.5-secure-authentication
```

A passing run reports this control green; in CI, `concord gate` exits non-zero while it fails.

## Cross-framework mappings

```
  iso27001: ["A.8.5"]
  soc2: ["CC6.1"]
  nist_csf: ["PR.AA-03"]
  hipaa: ["164.308(a)(5)(ii)(C)"]
  cis_controls: ["6.3", "6.4"]
```
